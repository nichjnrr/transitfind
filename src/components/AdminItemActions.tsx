// src/components/AdminItemActions.tsx
"use client";
// js using it as placeholder first
export default function AdminItemActions({
  itemId,
  itemType,
  status,
}: {
  itemId: string;
  itemType: "lost" | "found";
  status: string;
}) {
  return null;
}
